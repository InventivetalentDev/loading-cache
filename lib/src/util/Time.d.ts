export declare class Time {
    static millis(m: number): number;
    static seconds(s: number): number;
    static minutes(m: number): number;
    static hours(h: number): number;
    static get now(): number;
}
